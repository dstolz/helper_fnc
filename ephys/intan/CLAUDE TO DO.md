# TO DO

## Tasks

Instructions: Review the following tasks and implement them in the most efficient order. You may find it helpful to break down larger tasks into smaller sub-tasks or combine related tasks. After completing each task, mark it as done and update the status of the project accordingly. Clear your memory of completed tasks to maintain focus on the remaining items.


- [x] The `IntanKilosortApp` should maintain a manifest for each dataset including probe info (and other metadata) and the path to the Kilosort output files. This manifest should be stored in a JSON file in the dataset directory. The manifest should be updated whenever new Kilosort output files are generated or other relevant metadata changes. Make sure the "Datasets" table in the GUI reflects the current state of the manifest when scanned.
  - Manifest lives at `<Folder>/<Name>_manifest.json`. Read/written by `IntanDataset.writeManifest` / `applyManifest` / `manifestStruct` (filesystem state via `DatasetTracker`). The app writes it on scan, probe assignment, channel-exclusion change, `.bin`/Kilosort runs (`onRunBatch`) and on background-run completion (`pollKSRuns`). On scan, `applyManifest` restores the saved probe + channel exclusions, and the Datasets table now shows `Format`, `Probe`, `Exclude`, `Bin`, and `Kilosort` columns reflecting that state.
- [x] Move the "Probe" tab to the right of "Datasets" in the GUI.
- [x] Add a "Show Channel Numbers" checkbox to the "Probe" tab. When checked, the channel numbers should be displayed on the probe layout. When unchecked, the channel numbers should be hidden.
  - Checkbox `ShowChanNumbersCheckBox` (Probe tab) labels each site with its 1-based `.bin` channel in the preview plot.
- [x] Move "Open in phy" button to the "Datasets" tab. When clicked, it should open the phy GUI with the current dataset loaded. Deactivate the "Open in phy" button if no dataset is selected or if the selected dataset does not have Kilosort output files.
  - Button is on the Datasets tab; `updatePhyButtonState` enables it only when the selected dataset has a `kilosort4/params.py`.
- [x] Move the "Visualize" tab to the right of "Artifacts" tab in the GUI. Make sure to display detected artifacts in the "Visualize" tab plot if any are present. If no artifacts are detected. This should be compatible with the current artifact detection functionality.
  - Tab order is now Datasets, Probe, Artifacts, Visualize, Kilosort, Review. On Plot, the automatic detector (`IntanDataset.detectArtifacts`, same config as the Artifacts tab) runs on the loaded window; detected periods are shaded orange, manual periods red.
- [x] 'IntanDataset' already supports reading different Intan recording formats. Ensure that the `IntanKilosortApp` can handle datasets with different recording formats and update the manifest accordingly.
  - `IntanDataset.detectFormat` classifies each folder (traditional `*.rhd` / one-file-per-signal / one-file-per-channel / unknown); `RecordingFormat` is shown in the Datasets table and recorded in the manifest.
  - DONE: readers for the split formats are now implemented. `IntanDataset.splitLayout` parses `info.rhd` and maps the flat int16 `.dat` file(s) (`amplifier.dat` / `amp-<native>.dat`), deriving the sample count from file size; `readSplitWindow` reads any amplifier sample window (microvolts = `0.195 * int16`, no 32768 offset). All data access flows through two format-agnostic primitives — `streamPlan` (one chunk per `.rhd` file for traditional; bounded sample windows over the `.dat` for split) and `readChunkUV` (returns a `[nSamp x nChan]` microvolt chunk) — so `toBin`, `analyzeArtifacts`, and the Visualize tab (`onPlotVisualization`) are identical across formats with peak memory bounded. `refreshMetadata` sizes split recordings from `.dat`; `readData` delegates to `readSplitAll` (amplifier + dig-in events for both layouts, board ADC/aux for one-file-per-signal). Project discovery already finds split folders (`info.rhd` matches `*.rhd`). Covered by `test_IntanDataset` section 10 (both split layouts: metadata, `readData` amplifier+events, byte-correct `toBin`).
